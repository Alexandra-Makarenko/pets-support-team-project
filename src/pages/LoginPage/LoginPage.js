import { LoginForm } from 'components/Auth/LoginForm/LoginForm';

const LoginPage = () => {
  return (
    <main>
      <title>Login</title>
      <LoginForm />
    </main>
  );
};
export default LoginPage;
