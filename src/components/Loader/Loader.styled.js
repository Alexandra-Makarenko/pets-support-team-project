import styled from 'styled-components';
import { InfinitySpin } from 'react-loader-spinner';

export const LoaderWrapper = styled.div`
  width: 200px;
  margin: 0 auto;
  padding: 50px 0;
`;

export const LoaderIco = styled(InfinitySpin)``;
